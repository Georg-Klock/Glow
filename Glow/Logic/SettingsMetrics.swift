import CoreGraphics

/// Geometry the Settings screen deliberately fixes rather than inheriting
/// from the bounds of whichever SF Symbol happens to be selected.
enum SettingsMetrics {
    static let exportIconSize: CGFloat = 15
    static let exportIconFrame: CGFloat = 18
}
