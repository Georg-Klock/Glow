{
  "_": [
    "What a test run has to contain to count as one. Read by",
    "Tools/validate-test-result.py, which Tools/test.sh runs after every run.",
    "",
    "bundles: every test bundle the scheme is supposed to run, and the smallest",
    "number of tests it may report. A floor, not an equality — adding tests never",
    "touches this file. Lowering one means tests were deleted, and that should be",
    "as visible in a diff as deleting them was. A bundle that runs and is not",
    "listed here fails too: an undeclared target is a target that can vanish",
    "again unnoticed.",
    "",
    "diagnosticAllowlist: compiler and analyzer warnings this repository has",
    "decided to live with, each fingerprinted as 'issueType: message', with a",
    "reason and the issue tracking it. Use fingerprintPrefix only where the",
    "message carries an absolute path and so cannot be named exactly. Short is",
    "the intended state.",
    "",
    "requiredAttachments / visualFailureAttachments: evidence the run has to",
    "leave behind. The render baseline records what it rendered on every run,",
    "and a visual failure has to leave images a person can look at. See #138.",
    "",
    "What that render baseline covers, because this file read as covering more",
    "than it did (#199). Three claims, each with its own statistic in",
    "RenderTests/Baselines/render-signatures.json:",
    "",
    "  geometry — a 16 x 16 grid of mean brightness. A mark that moves a",
    "  column, a row pitch that shifts, a label that stops being drawn.",
    "  ground — the share of the frame that is exactly 0,0,0. A gradient, a",
    "  tint or a material behind the marks.",
    "  tone — a census of the exact levels the app paints and how many pixels",
    "  it paints at each. The grid alone could not do this: it is a mean, and",
    "  the marks that carry the unlit colour are thin, so #194 moved the whole",
    "  palette thirteen levels and no cell moved more than three.",
    "",
    "Not covered: the glow. The simulator has no EDR headroom, so every frame",
    "in the baseline is the SDR render. GlowRendererTests holds that claim.",
    "",
    "Which surfaces those three statistics are measured on, for the same",
    "reason (#386): the widget families; three isolated app frames — grid rows,",
    "grid rows editing and weekday header — drawn by the real HabitRowView and",
    "WeekdayHeader; and two whole production screens, WeeklyGridView and",
    "WidgetsView. ImageRenderer cannot flatten either screen's NavigationStack,",
    "so HostedScreenFrames uses a UIWindow and drawHierarchy with a pinned",
    "393 x 852 surface, that surface's 59pt top and 34pt bottom safe area, 2x",
    "display/output scale, dark appearance, fixed Tuesday and the same nine-row",
    "fixture in real SwiftData. The safe area is corrected before the host enters",
    "the live scene, so a simulator model cannot move the navigation layout.",
    "The hosted signature gates the initially visible viewport, including the",
    "weekly list surround and the Widgets screen through Large and the start of",
    "Medium. It does not claim offscreen scroll content. Lower monthly cards",
    "remain covered individually by MonthWidgetView frames and grouping tests.",
    "",
    "visualFailureAttachments: what a non-passing render bundle owes a reviewer,",
    "per frame rather than per run. Evidence is named <frame>-actual.png,",
    "<frame>-expected.png, <frame>-diff.png, so grouping on that prefix tells a",
    "frame that moved from a frame that is new: `always` is required of every",
    "frame that attached anything, and the `together` pair must be all present",
    "or all absent. A move shows three images; an addition has no expected and",
    "no diff to show and shows its render alone. Requiring all three of the run",
    "refused every addition, which closed the route RenderBaselineTests",
    "documents for adding a family. See #385.",
    "",
    "release: what a built product has to contain to be shippable. Read by",
    "Tools/check-release-build.py, which CI runs on the unsigned Release build",
    "and Tools/ship-testflight.sh runs on the archive and on the exported .ipa.",
    "The bundle identifiers and the App Group are declared here rather than",
    "argued for at each call site, so the gate and the release path cannot",
    "disagree about what they are checking. An appex that ships and is not",
    "listed fails, for the same reason an undeclared test bundle does. See #133."
  ],
  "bundles": {
    "GlowTests": { "minimum": 449 },
    "GlowRenderTests": { "minimum": 13 },
    "GlowUITests": { "minimum": 1 }
  },
  "release": {
    "appGroup": "group.com.georgklock.glow",
    "host": {
      "bundleIdentifier": "com.georgklock.glow",
      "requiredResources": ["PrivacyInfo.xcprivacy"]
    },
    "extensions": {
      "GlowWidget.appex": {
        "bundleIdentifier": "com.georgklock.glow.widget",
        "requiredResources": ["PrivacyInfo.xcprivacy"]
      }
    }
  },
  "requiredAttachments": ["render-signatures-actual.json"],
  "visualFailureAttachments": {
    "bundle": "GlowRenderTests",
    "always": "actual",
    "together": ["expected", "diff"]
  },
  "diagnosticAllowlist": [
    {
      "fingerprintPrefix": "Uncategorized: Aggregation tool emitted warnings:",
      "reason": "llvm-profdata complains that a coverage profile was produced by a different binary than the one being merged. It is a property of a DerivedData directory that a previous run wrote into, not of the code: a clean CI runner never produces it, and a developer who edits a test between two local runs always does. Its message quotes an absolute path, so it cannot be fingerprinted exactly.",
      "issue": "#138"
    }
  ]
}
